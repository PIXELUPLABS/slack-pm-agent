# Required, set your Anthropic API key.
ANTHROPIC_API_KEY=YOUR_ANTHROPIC_API_KEY

# ClickUp & Fireflies MCP servers authenticate via OAuth — run once:
#   npm run auth:clickup
#   npm run auth:fireflies
# Tokens persist in data/mcp-auth/ (gitignored) and refresh automatically.
# The agent gets read-only tools; writes run through the approval executor.
#
# Optional overrides: a static bearer token skips OAuth for that server, and
# the URL can point at a different MCP endpoint.
# CLICKUP_MCP_TOKEN=YOUR_CLICKUP_MCP_TOKEN
# CLICKUP_MCP_URL=https://mcp.clickup.com/mcp
# FIREFLIES_MCP_TOKEN=YOUR_FIREFLIES_MCP_TOKEN
# FIREFLIES_MCP_URL=https://api.fireflies.ai/mcp
# MCP_OAUTH_CALLBACK_PORT=8976

# Optional, uncomment and set when running without the Slack CLI (npm start).
# SLACK_APP_TOKEN=YOUR_SLACK_APP_TOKEN
# SLACK_BOT_TOKEN=YOUR_SLACK_BOT_TOKEN

# Optional, uncomment and set for OAuth mode (node app-oauth.js).
# SLACK_CLIENT_ID=YOUR_SLACK_CLIENT_ID
# SLACK_CLIENT_SECRET=YOUR_SLACK_CLIENT_SECRET
# SLACK_SIGNING_SECRET=YOUR_SLACK_SIGNING_SECRET
# SLACK_REDIRECT_URI=https://YOUR_NGROK_SUBDOMAIN.ngrok-free.app/slack/oauth_redirect

# Optional, uncomment and set when using a custom Slack instance.
# SLACK_API_URL=YOUR_SLACK_API_URL
